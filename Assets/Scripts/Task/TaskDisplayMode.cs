using UnityEngine;

// Перечисление режимов отображения
public enum TaskDisplayMode
{
    AlwaysVisible,      // Всегда видимый
    PopupMode,          // Всплывающий режим с кнопками
    DisabledInScene     // Отключен в этой сцене
}